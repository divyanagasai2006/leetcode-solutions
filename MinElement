class Solution:
    def minElement(self, nums: List[int]) -> int:
        mini=float('inf')
        for num in nums:
            total=0
            while num>0:
                digit=num%10
                total=total+digit
                num=num//10
            mini=min(mini,total)
        return mini
