class Solution:
    def minimumCost(self, cost: List[int]) -> int:
        cost.sort(reverse=True)
        total=0
        count=0
        for candy in cost:
            count+=1
            if count%3!=0:
                total+=candy
        return total

        
